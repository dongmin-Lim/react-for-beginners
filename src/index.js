import React from "react";
import ReactDOM from "react-dom/client"; // 바뀐 부분
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root")); // 바뀐 부분
root.render(
  <>
    <App />
  </>
);
